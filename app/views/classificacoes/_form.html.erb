<%= form_with(model: classificacao, local: true) do |form| %>
  <% if classificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classificacao.errors.count, "error") %> prohibited this classificacao from being saved:</h2>

      <ul>
      <% classificacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :idade_minima %>
    <%= form.number_field :idade_minima %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
